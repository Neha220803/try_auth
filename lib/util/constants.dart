import 'package:flutter/material.dart';

const Color midnightBlue = Color(0xFF222671);

/// Delft Blue - variant of blue from the logo
const Color delftBlue = Color(0xFF373566);

/// Bittersweet - red color from the banner
const Color bittersweet = Color(0xFFFA4E4E);

/// Orange - from the logo/flag
const Color orange = Color(0xFFF18121);

/// Lion - golden/tan color from the emblem
const Color lion = Color(0xFFB6966F);

/// White - for text on dark backgrounds
const Color white = Colors.white;

/// Black - for text on light backgrounds
const Color black = Colors.black;

/// Light variant of Midnight Blue for container backgrounds
const Color lightMidnightBlue = Color(0xFF2A2E85);

/// Light variant of Delft Blue for secondary container backgrounds
const Color lightDelftBlue = Color(0xFF43417A);

const Color primarycolor = Color(0xFF222671);

const Color opaquePrimarycolor = Color.fromARGB(143, 34, 38, 113);
